<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="">
    <style>
        body {
            background: linear-gradient(270deg, #1e3a8a, #6b21a8, #9333ea, #3b82f6);
            background-size: 400% 400%;
            animation: backgroundAnimation 15s ease infinite;
            overflow: hidden;
            background-image: radial-gradient(circle, rgba(255,255,255,0.05) 1px, transparent 1px),
                              radial-gradient(circle, rgba(255,255,255,0.05) 1px, transparent 1px);
            background-color: #1e3a8a;
            background-size: 10px 10px;
        }
    </style>
</head>
<body class="min-h-screen flex items-center justify-center bg-gray-900">

    <!-- Pattern Effect -->
    <div class="w-full min-h-screen flex items-center justify-center p-6">
        <form 
            action="/login" 
            method="post" 
            class="w-full max-w-md bg-zinc-800 rounded-lg p-6 shadow-lg transform transition-all duration-500 animate-fadeIn"
        >
            <h2 class="text-3xl font-semibold text-white text-center mb-6">
               Login to Account
            </h2>

            <!-- Email Field -->
            <div class="mb-4">
                <label class="block text-white text-sm font-medium mb-2" for="email">Email</label>
                <input 
                    type="email" 
                    id="email" 
                    name="email" 
                    placeholder="Enter your email"
                    class="w-full px-4 py-2 bg-zinc-700 text-white rounded-lg border border-zinc-600 focus:outline-none focus:border-blue-500"
                    required
                />
            </div>

            <!-- Password Field -->
            <div class="mb-4">
                <label class="block text-white text-sm font-medium mb-2" for="password">Password</label>
                <input 
                    type="password" 
                    id="password" 
                    name="password" 
                    placeholder="Enter a strong password"
                    autocomplete="new-password"
                    class="w-full px-4 py-2 bg-zinc-700 text-white rounded-lg border border-zinc-600 focus:outline-none focus:border-blue-500"
                    required
                />
            </div>

            <!-- Submit Button -->
            <button 
                type="submit" 
                class="w-full bg-blue-600 hover:bg-blue-700 text-white font-medium py-2 px-4 rounded-lg transition-all duration-200"
            >
               Login
            </button>
            <div class="mt-4 mr-4 flex justify-end items-center space-x-2">
                <p class="text-gray-300 text-lg">Not registered yet?</p>
                <a href="/" class="bg-gradient-to-r from-teal-400 to-blue-500 text-white text-lg py-1 px-4 rounded-lg shadow-lg hover:bg-gradient-to-l hover:from-blue-500 hover:to-teal-400 transform transition duration-300 hover:scale-105">
                    Register
                </a>
            </div>
            
        </form>
     
    </div>
   


</body>
</html>
